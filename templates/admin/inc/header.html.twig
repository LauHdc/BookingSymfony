<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
		
  		<div class="container-fluid">
			
    		<a class="navbar-brand" href="#">R'BNB</a>
    		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      			<span class="navbar-toggler-icon"></span>
    		</button>

		<div class="collapse navbar-collapse" id="navbarColor01">
			
      		<ul class="navbar-nav me-auto">
        		<li class="nav-item">
          			<a class="nav-link" href="{{path('admin_dashboard')}}"><i class="fas fa-chart-line"></i> Tableau de bord</a>
        		</li>
				
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin_ads_list')}}"><i class="fas fa-hotel"></i> Annonces</a>
				</li>
				
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin_bookings_list')}}"><i class="fas fa-folder"></i> Réservations</a>
				</li>
				
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin_comments_list')}}"><i class="fas fa-comments"></i> Commentaires</a>
				</li>
				
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin_users_list')}}"><i class="fa fa-users"></i> Utilisateurs</a>
				</li>
			</ul>
        		
			<ul class="navbar-nav align-items-center" ml-auto>
				
				{% if app.user %}
							
					<li class="nav-item">
						<a href="{{path('homepage')}}" class="nav-link" target="_blank"><i class="fas fa-eye"></i> Site</a>
					</li>
				
					<li class="nav-item">	
						<a class="nav-link" href="{{path('admin_account_logout')}}"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
					</li>
					
					<img src="{{app.user.avatar}}" alt="Avatar de {{app.user.fullName}}" class="rounded-circle mt-1" style="width: 37px; height: 37px;">
				  
					<li class="nav-item">
						<a class="nav-link" href="{{path('account_home')}}">{{app.user.fullName}}</a>
					</li>
				
				{% else %}
				
				<li class="nav-item">
				  <a class="nav-link" href="{{path('account_register')}}">Inscription</a>
				</li>
				
				<li class="nav-item">
				  <a class="nav-link" href="{{path('account_login')}}">Connexion</a>
				</li>
				
				{% endif %}

      		</ul>
			
    	</div>
			
		</div>
		
</nav>